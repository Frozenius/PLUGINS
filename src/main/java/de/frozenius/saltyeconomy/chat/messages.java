package de.frozenius.saltyeconomy.chat;

import org.bukkit.ChatColor;

public class messages {

    public static String pluginPrefix(){
        return ChatColor.DARK_GRAY + "[" + ChatColor.GOLD + "SALTY ECONOMY" + ChatColor.DARK_GRAY + "] " + ChatColor.GRAY;
    }

    public static String errorPrefix(){
        return ChatColor.DARK_GRAY + "[" + ChatColor.RED + "ERROR" + ChatColor.DARK_GRAY + "] " + ChatColor.GRAY;
    }

}
